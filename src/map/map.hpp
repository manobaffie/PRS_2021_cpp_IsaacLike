#include <iostream>
// #include <SFML/Graphics.hpp>

class map
{
    private:
       std::string p_texture;

        // sf::Texture texture;
        // sf::Sprite sprite;

    public:
        map(std::string t);
        ~map();
};
